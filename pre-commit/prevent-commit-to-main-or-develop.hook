#!/bin/sh
# Prevent commits to main or develop branches
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "develop" ]; then
  echo "\033[0;31mERROR: Direct commits to $branch are not allowed. Please use a feature branch.\033[0m"
  exit 1
fi